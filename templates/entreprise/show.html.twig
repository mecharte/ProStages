{% extends 'base.html.twig' %}

{% block contenu %}
    <h1>Entreprise</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entreprise.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ entreprise.nom }}</td>
            </tr>
            <tr>
                <th>Activite</th>
                <td>{{ entreprise.activite }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ entreprise.adresse }}</td>
            </tr>
            <tr>
                <th>SiteWeb</th>
                <td>{{ entreprise.siteWeb }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('entreprise_index') }}">retour à la liste des entreprises</a>
    <br>

    <a href="{{ path('entreprise_edit', {'id': entreprise.id}) }}">editer</a>

    {{ include('entreprise/_delete_form.html.twig') }}
{% endblock %}
