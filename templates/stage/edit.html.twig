{% extends 'base.html.twig' %}

{% block contenu %}
    <h1>Editer Stage</h1>

    {{ include('stage/_form.html.twig', {'button_label': 'Mettre à jour la modification'}) }}

    <a href="{{ path('stage_index') }}">retour à la liste des stages</a>

    {{ include('stage/_delete_form.html.twig') }}
{% endblock %}
