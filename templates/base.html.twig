<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="{{asset('/css/bootstrap.min.css')}}">
    <link rel="stylesheet" href="{{asset('/css/fontello.css')}}">
</head>

<body>
    {% block slider %}
    <div class="col-md-12">
        <blockquote class="blockquote text-center">
            <p class="mb-0">
                <h1>
                    <center><i class="icon-lastfm-1"></i><a href="{{path('proStage')}}"
                            style="color:darkgoldenrod; text-decoration: none">Prostages</a>
                    </center>
                </h1>
            </p>
            <h3>
            </h3>
        </blockquote>
    </div>
    {% endblock %}
    <br><br>
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-9">
                {% block contenu %}
                {% endblock %}
            </div>


            {% block menu %}
            <div class="col-md-3">
                    {% if is_granted('ROLE_USER') %}
                    {{ app.user.prenom }}
                    {{ app.user.nom }}
                    <i class="icon-user"></i>
                    <a href="{{path('app_logout')}}">Déconnexion</a>
                    {% else %}
                    <i class="icon-user"></i>
                    <a href="{{path('app_login')}}">Connexion</a>
                {% endif %}
                
                
                    
                    <br>
                    <br>
                <ul class="list-group">
                    <li class="list-group-item">
                        <h1>
                            <center> Filtrer par </center>
                        </h1>
                    </li>
                    <li class="list-group-item"><a href="{{path('entreprises')}}">Entreprises</a> </li>
                    <li class="list-group-item"><a href="formation">Formations</a> </li>
                </ul>
                <br>
                <br>
                <a href="{{path('formulairePourEntreprise')}}">Rajouter une entreprise</a> <br> <br>
                <a href="{{path('ajouterUnStage')}}">Rajouter un stage</a>
                
            </div>
        </div>
                {% endblock %}
            

        


        {% block footer %}
        <br>
        <br>
        <h6 align="right">Site réalisé par Mathieu Echarte avec les frameworks <a href="https://symfony.com">Symfony</a>
            et <a href="https://getbootstrap.com">Bootstrap</a></h6>
        {% endblock %}
    </div>
    {% block javascripts %}
    {% endblock %}
    
</body>

</html>